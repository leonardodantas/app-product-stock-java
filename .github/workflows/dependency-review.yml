name: Meu Fluxo de Trabalho

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código-fonte
        uses: actions/checkout@v2

      - name: Executar script JavaScript
        run: node .github/scripts/meu_script.js
